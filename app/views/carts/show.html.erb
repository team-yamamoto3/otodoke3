  <h1>注文内容を確認・変更する</h1>

  <h4>支払い方法の選択</h4>
  <%= form_with(model: @receipts, local: true) do |form| %>
    <%= form.radio_button :payment, :bank %>
    <%= form.label :payment, '銀行振込', {value: :bank, style: "display: inline-block;"} %>
    <%= form.radio_button :payment, :kurejittokado %>
    <%= form.label :payment, 'クレジットカード', {value: :kurejittokado, style: "display: inline-block;"} %>
    <%= form.radio_button :payment, :daibiki %>
    <%= form.label :payment, '代引き', {value: :daibiki, style: "display: inline-block;"} %>
    <%= form.hidden_field :order_status %>

  <h4>配送先選択</h4>
  <h4>既存の登録先住所</h4>
  <%= current_enduser.user_address %>
    <%# <%= form.text_area :user_address, :placeholder => "#{current_enduser.user_address}" %> %>
    <br>
    <%= submit_tag %>

  <h3>注文内容</h3>

  <h4>小計</h4>
  <% array = [] %>
  <% @carts.each do |cart| %>
  <% total = cart.cd&.price * cart.cd&.consumption_tax %>
  <p>¥<%=  totalcd = total.floor * cart.cartnumber %>（税込）</p>
  <% array << totalcd %>
  <% end %>
  <%= array.sum %>円
  <%# <%= form.hidden_field :sum_price, value: @receipt.sum_price %> %>
  <%# <%= hidden_field_tag :sum_price, @receipt.sum_prices %> %>
  <h4>配送料</h4>
  <%= form.hidden_field :postage %>
  <p>500円</p>
  <h2>注文内容合計</h2>
  <p style="color: #e82357e3; font-weight: bold; font-size: 30px;">¥<%= array.sum + 500 %>円!!!!!!</p>
  <% end %>
